import { ComingSoon } from '../../../components/shared/ComingSoon';

export default function AdminSettingsPage() {
  return (
    <ComingSoon
      title="Paramètres"
      description="Les paramètres globaux de la plateforme seront bientôt disponibles."
      backHref="/admin"
    />
  );
}

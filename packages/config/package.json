{
  "name": "@liguita/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Référentiels Liguita : pays, villes, quartiers, lieux, catégories, types d'objets.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./categories": "./src/categories.ts",
    "./places": "./src/places.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.node.json",
    "lint": "eslint src"
  },
  "dependencies": {
    "@liguita/core": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
